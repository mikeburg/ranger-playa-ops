#!/bin/sh

if [[ $# -eq 0 ]] ; then
  echo "Supply a service name to rebuild: api, classic, client, db, smtpd"
  exit 1
fi

echo "** Building service $@"

docker-compose -f docker-compose.yml -f secrets.yml build "$@"
